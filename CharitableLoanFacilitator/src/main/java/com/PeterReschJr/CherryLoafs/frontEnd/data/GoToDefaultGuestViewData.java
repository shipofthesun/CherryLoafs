package com.PeterReschJr.CherryLoafs.frontEnd.data;

/**
 * A GoToDefaultGuestViewData represents a signal from the FrontEnd that a Guest wishes to
 * return to the default landing view for guests visiting the application (DefaultGuestView).
 * Currently holds no actual data.
 * 
 * @author peter
 *
 */
public class GoToDefaultGuestViewData extends ViewData {
	
}
