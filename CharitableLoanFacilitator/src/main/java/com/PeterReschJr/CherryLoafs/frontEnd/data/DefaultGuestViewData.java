package com.PeterReschJr.CherryLoafs.frontEnd.data;

/**
 * A DefaultGuestViewData represents data needed to populate a FrontEnd DefaultGuestView.
 * It also serves as a signal to the FrontEnd that when an object of this type is returned, the
 * DefaultGuestView should be displayed populated with any data in DefaultGuestViewData.
 * Currently contains no data as their are no fields specific to a Guest that need be filled.
 * 
 * @author peter
 *
 */
public class DefaultGuestViewData extends ViewData {
	
}
